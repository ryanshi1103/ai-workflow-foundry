#!/bin/bash
# cc-projects-maintain — project maintenance (now flowfoundry project maintain)
# Thin wrapper — delegates to the unified flowfoundry CLI.
set -euo pipefail

exec python3 -m flowfoundry.workspace.maintain_cli "$@"

# Codex GPT-5.6 Sol — Manual profile
# Approval: on-request (model decides when to ask)
# Sandbox: workspace-write (project dir writable)

model = "gpt-5.6-sol"
model_reasoning_effort = "high"
model_reasoning_summary = "auto"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
